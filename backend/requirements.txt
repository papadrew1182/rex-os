# Rex OS — Backend Dependencies
# Primary target: Python 3.12+
# Tested: 3.12, 3.14
fastapi>=0.115.0,<0.136
# Pinned <1.0 because starlette 1.0.0 changed TestClient event-loop
# semantics in a way that breaks the shared asyncpg pool across tests
# under pytest-asyncio session-scoped loops (CI run 24369741252).
# Unpin once conftest owns its own engine per-session cleanly.
starlette>=0.40,<1.0
# anyio 4.13 moved loop management in a way that similarly tangles with
# the session-scoped event loop we rely on in pytest.ini. Hold at 4.12.x.
anyio>=4.12,<4.13
uvicorn[standard]>=0.30.6
asyncpg>=0.29.0
pydantic>=2.9.2
pydantic-settings>=2.5.2
python-dotenv>=1.0.1
httpx>=0.27.2
python-multipart>=0.0.12
sqlalchemy[asyncio]>=2.0.36
bcrypt>=4.0
apscheduler>=3.10.0
# Phase 43: object storage (S3 / R2 / MinIO) — only constructed when
# REX_STORAGE_BACKEND=s3; local mode never imports it at runtime.
boto3>=1.34.0
# Phase 44: login rate limiting + error tracking
slowapi>=0.1.9
sentry-sdk[fastapi]>=2.14.0
# Testing
pytest>=8.0
pytest-asyncio>=0.24
