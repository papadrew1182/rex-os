"""Top-level schemas package for the AI spine lane (Session 1, feat/ai-spine).

Sibling to the existing ``app/schemas/`` (which holds the SQLAlchemy-domain
DTOs). The AI spine owns its own request/response contracts so they can be
frozen independently for Session 2 and Session 3 consumers.
"""
